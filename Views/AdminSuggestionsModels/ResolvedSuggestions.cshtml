@model IEnumerable<College.Areas.Identity.Data.SuggestionsModel>

@{
    ViewData["Title"] = "ResolvedSuggestions";
}

<h1>ResolvedSuggestions</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SuggestionMessage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SuggestionMessage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SuggestionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SuggestionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SuggestionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
